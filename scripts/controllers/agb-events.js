'use strict';

angular.module('dreambeemcomApp')
    .controller('AgbEventsCtrl', function ($scope, $location, stkMetaService) {
        stkMetaService.setTitle('Dreambeem.com - Power Market | AGBs Events');
        stkMetaService.setDescription('');
    });
