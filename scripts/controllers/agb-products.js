'use strict';

angular.module('dreambeemcomApp')
    .controller('AgbProductsCtrl', function ($scope, $location, stkMetaService) {
        stkMetaService.setTitle('Dreambeem.com - Power Market | AGBs Shops');
        stkMetaService.setDescription('');
    });
